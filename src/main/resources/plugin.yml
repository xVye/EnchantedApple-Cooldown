name: "${bukkit.plugin.name}"
prefix: "${bukkit.plugin.prefix}"
description: "${bukkit.plugin.description}"

main: "${bukkit.plugin.main}"
version: "${bukkit.plugin.version}"

authors: [SirBlobman, xVye]

softdepend:
  - "MVdWPlaceholderAPI"
  - "PlaceholderAPI"